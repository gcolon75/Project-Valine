import useInfiniteList from '../hooks/useInfiniteList';import * as api from '../services/api';export default function Auditions(){const {items,sentinel}=useInfiniteList((p)=>api.listAuditions(p),[]);return(<div><h2>Auditions</h2>{items.map(a=><div key={a.id} className='card'><a href={'/auditions/'+a.id}><b>{a.title}</b></a><p>{a.summary}</p></div>)}<div ref={sentinel} style={{height:1}}></div></div>);}
