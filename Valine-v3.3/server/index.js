import express from 'express';import cors from 'cors';import cookieParser from 'cookie-parser';import auth from './routes/auth.js';import feed from './routes/feed.js';import scripts from './routes/scripts.js';import auditions from './routes/auditions.js';import search from './routes/search.js';import requests from './routes/requests.js';import comments from './routes/comments.js';import notifications from './routes/notifications.js';import messages from './routes/messages.js';const app=express();const PORT=process.env.PORT||4000;const ORIGIN=process.env.ORIGIN||'http://localhost:5173';app.use(cors({origin:ORIGIN,credentials:true}));app.use(express.json());app.use(cookieParser());app.use('/auth',auth);app.use('/feed',feed);app.use('/scripts',scripts);app.use('/auditions',auditions);app.use('/search',search);app.use('/requests',requests);app.use('/scripts',comments);app.use('/notifications',notifications);app.use('/messages',messages);app.get('/',(_req,res)=>res.json({ok:true}));app.listen(PORT,()=>console.log('API running on',PORT));
