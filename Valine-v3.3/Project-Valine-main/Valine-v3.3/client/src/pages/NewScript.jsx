import { useState } from 'react';import * as api from '../services/api';import { useToast } from '../context/ToastContext';export default function NewScript(){const [title,setTitle]=useState('');const [summary,setSummary]=useState('');const { push }=useToast();const create=async()=>{await api.createScript({title,summary,isPrivate:true});push('Script created');window.location.href='/scripts';};return(<div className='card'><h2>Post Script</h2><input placeholder='Title' value={title} onChange={e=>setTitle(e.target.value)} /><textarea rows='4' placeholder='Summary' value={summary} onChange={e=>setSummary(e.target.value)}/><div><button className='btn primary' onClick={create}>Create</button></div></div>);}
