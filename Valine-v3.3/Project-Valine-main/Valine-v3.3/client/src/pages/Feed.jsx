import useInfiniteList from '../hooks/useInfiniteList';import * as api from '../services/api';export default function Feed(){const {items,sentinel}=useInfiniteList((p)=>api.feed(p),[]);return(<div><h2>Feed</h2>{items.map((it,i)=><div key={i} className='card'><b>{it.title}</b><p>{it.summary}</p><div style={{color:'var(--muted)'}}>{it.kind}</div></div>)}<div ref={sentinel} style={{height:1}}></div></div>);}
