import { createContext,useState,useContext,useCallback } from 'react';
const C=createContext(null);export function ToastProvider({children}){const [toasts,setToasts]=useState([]);const push=useCallback((msg)=>{const id=Date.now();setToasts(t=>[...t,{id,msg}]);setTimeout(()=>setToasts(t=>t.filter(x=>x.id!==id)),2500);},[]);return <C.Provider value={{push}}><div>{children}</div><div style={{position:'fixed',bottom:10,right:10,display:'flex',flexDirection:'column',gap:8}}>{toasts.map(t=><div key={t.id} className='card'>{t.msg}</div>)}</div></C.Provider>; }export const useToast=()=>useContext(C);
