// TODO: Re-enable SocialProofSection when real social proof is available.
// Commented out for marketing release per product owner request.
const SocialProofSection = () => {
  return null;
};

export default SocialProofSection;
