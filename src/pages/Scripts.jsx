import useInfiniteList from '../hooks/useInfiniteList';import * as api from '../services/api';export default function Scripts(){const {items,sentinel}=useInfiniteList((p)=>api.listScripts(p),[]);return(<div><h2>Scripts</h2>{items.map(s=><div key={s.id} className='card'><a href={'/scripts/'+s.id}><b>{s.title}</b></a><p>{s.summary}</p></div>)}<div ref={sentinel} style={{height:1}}></div></div>);}
