import { useState } from 'react';import * as api from '../services/api.js';import { useToast } from '../context/ToastContext';export default function NewAudition(){const [title,setTitle]=useState('');const [summary,setSummary]=useState('');const { push }=useToast();const create=async()=>{await api.createAudition({title,summary});push('Audition posted');window.location.href='/auditions';};return(<div className='card'><h2>Post Audition</h2><input placeholder='Title' value={title} onChange={e=>setTitle(e.target.value)} /><textarea rows='4' placeholder='Description' value={summary} onChange={e=>setSummary(e.target.value)}/><div><button className='btn primary' onClick={create}>Create</button></div></div>);}
