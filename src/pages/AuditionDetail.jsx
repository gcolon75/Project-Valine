import { useEffect,useState } from 'react';import { useParams } from 'react-router-dom';import * as api from '../services/api.js';export default function AuditionDetail(){const { id }=useParams();const [a,setA]=useState(null);useEffect(()=>{api.getAudition(id).then(setA)},[id]);if(!a)return <div className='card'>Loading…</div>;return(<div className='card'><h2>{a.title}</h2><p>{a.summary}</p><button className='btn primary'>Apply</button></div>);}
