import { useState } from 'react';import * as api from '../services/api';export default function Search(){const [q,setQ]=useState('');const [r,setR]=useState([]);const go=async()=>setR(await api.search(q));return(<div><h2>Search</h2><div className='grid2'><input value={q} onChange={e=>setQ(e.target.value)} placeholder='Find scripts, auditions'/><button className='btn' onClick={go}>Search</button></div><ul>{r.map(x=><li key={x.kind+x.id}>{x.kind}:<a href={x.kind==='script'?('/scripts/'+x.id):('/auditions/'+x.id)}>{x.title}</a></li>)}</ul></div>);}
