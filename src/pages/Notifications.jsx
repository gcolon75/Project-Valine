import { useState,useEffect } from 'react';import * as api from '../services/api';import { useAuth } from '../context/AuthContext';export default function Notifications(){const {user}=useAuth();const [nots,setNots]=useState([]);useEffect(()=>{api.listNotifications(user.id).then(setNots).catch(()=>setNots([]));},[user]);return(<div><h2>Notifications</h2>{nots.map(n=>(<div key={n.id} className='card'>{n.message}</div>))||<p>No notifications</p>}</div>);}
