{
  "name": "valine-api",
  "type": "module",
  "private": true,
  "scripts": {
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate deploy",
    "seed": "node prisma/seed.js",
    "migrate:social-links": "node scripts/migrate-social-links.js",
    "migrate:social-links:dry-run": "node scripts/migrate-social-links.js --dry-run"
  },
  "dependencies": {
    "@prisma/client": "^6.19.0"
  },
  "devDependencies": {
    "prisma": "^7.0.0"
  }
}
